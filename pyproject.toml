[project]
name = "potato"
version = "0.1.0"
description = "Minimal standalone Edge TTS web tool"
requires-python = ">=3.11"
dependencies = [
    "edge-tts==7.2.7",
    "requests>=2.32,<3",
]

[tool.uv]
package = false
